Card — the default content surface. Use `title`+`action` for sectioned panels; `pad` for simple padded boxes; `hover` for clickable cards.

```jsx
<Card title="Jadwal Hari Ini" action={<Button variant="ghost" size="sm">Semua</Button>}>
  ...
</Card>
<Card pad hover>...</Card>
```