IconButton — icon-only action; always pass `label` for accessibility.

```jsx
<IconButton label="Notifikasi"><i data-lucide="bell" /></IconButton>
<IconButton solid label="Tambah"><i data-lucide="plus" /></IconButton>
```